// chat-bubble.js
(function () {
  const createChatHTML = () => {
    const chatContainer = document.createElement("div");
    chatContainer.id = "chat-container";
    chatContainer.classLlist.add("chat-hidden");

    const closeChatBtn = document.createElement("button");
    closeChatBtn.id = "close-chat-btn";
    closeChatBtn.style.position = "absolute";
    closeChatBtn.style.top = "10px";
    closeChatBtn.style.right = "10px";
    closeChatBtn.style.fontSize = "24px";

    chatContainer.appendChild(closeChatBtn);

    const svgSend = 
  };
});
